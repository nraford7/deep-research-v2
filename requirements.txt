# Deeper Research dependencies
# Install:  pip install -r requirements.txt

# Model SDKs
anthropic>=0.122.0   # claude-opus-5 streams thinking blocks by default; 0.122.0 is the floor verified to decode them
openai>=1.55.0
google-genai>=0.3.0

# HTTP + parsing (helper scripts)
requests>=2.31.0
mistune>=3.0.0,<4.0.0

# Full-text extraction for fetch_fulltext.py (reads OA papers / PDF white papers).
# Without it, PDF rows skip gracefully; HTML full text still works.
pypdf>=4.0.0

# Optional: faster fuzzy match for bibliography dedup
rapidfuzz>=3.6.0

# Optional: semantic search over your research — see requirements-search.txt
